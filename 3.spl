main
var a, b, c, d, e, f, g;
{
	let b <- 50;
	let c <- b;
	let a <- b + c;
	let d <- b + c;
	let e <- c + b + d;
	let x <- 3;
	
	if a == d then
		let e <- c + b + d;
		let f <- a * d;
		let g <- d * a + 4;
		let b <- 5;
		let x <- 4;
		let e <- c + b + d
	else
		let f <- a / d;
		let g <- d / a
		
	fi;
	
	let f <- a * d;
	let g <- d / a;
	call OutputNum (a);
	call OutputNum (e);
	call OutputNum (f);
	call OutputNum (g)
	
}.